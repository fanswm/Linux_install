## wordcloud
+ install anaconda2
> `source ~/bashrc`

+ `pip install wordcloud`

+ `pip install jieba`
+ `pip install pyldavis`

+ `pip install snownlp`
+ `pip install -U textblob`
+ `python -m textblob.download_corpora`
